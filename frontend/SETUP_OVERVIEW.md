# Redux + JWT Authentication - Setup Overview

## 🏗️ Complete Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     React Application                        │
│  main.tsx (Redux Provider Wrapper)                           │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│                      App.tsx                                 │
│  - Checks for saved JWT token on startup                    │
│  - Calls getCurrentUser() to validate token                 │
│  - Shows Loader while checking auth                         │
│  - Renders Routes component                                 │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│                  routes.tsx                                  │
│  ┌────────────────────────┐   ┌───────────────────────────┐ │
│  │   NOT AUTHENTICATED    │   │   AUTHENTICATED           │ │
│  ├────────────────────────┤   ├───────────────────────────┤ │
│  │  /login                │   │  /dashboard (Protected)   │ │
│  │  /register             │   │  /other-pages             │ │
│  │  /forgot-password      │   │  (Protected)              │ │
│  │  → AuthLayout          │   │  → DashboardLayout        │ │
│  └────────────────────────┘   │  + Logout Button          │ │
│                                └───────────────────────────┘ │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│              Redux Store (src/app/store.ts)                  │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ authReducer (src/features/auth/authSlice.ts)           ││
│  │ ┌───────────────────────────────────────────────────┐ ││
│  │ │ State:                                            │ ││
│  │ │  - user: User | null                             │ ││
│  │ │  - token: string (JWT)                           │ ││
│  │ │  - isAuthenticated: boolean                      │ ││
│  │ │  - isLoading: boolean                            │ ││
│  │ │  - error: string | null                          │ ││
│  │ │                                                   │ ││
│  │ │ Async Actions:                                   │ ││
│  │ │  - login(email, password)                        │ ││
│  │ │  - register(name, email, password)               │ ││
│  │ │  - getCurrentUser()                              │ ││
│  │ │  - logout()                                      │ ││
│  │ └───────────────────────────────────────────────────┘ ││
│  └─────────────────────────────────────────────────────────┘│
└──────────────────────┬──────────────────────────────────────┘
                       │
        ┌──────────────┴──────────────┐
        │                             │
        ▼                             ▼
┌────────────────────┐    ┌─────────────────────┐
│  Components use:   │    │  Storage:           │
│                    │    │                     │
│ useAuth() hook     │    │  localStorage       │
│ - user             │    │  └─ authToken (JWT) │
│ - login()          │    │                     │
│ - logout()         │    │  sessionStorage     │
│ - register()       │    │  └─ Optional        │
│ - error            │    │                     │
│ - isLoading        │    │  Redux Store        │
│ - isAuthenticated  │    │  └─ Full auth state │
└────────────────────┘    └─────────────────────┘
                       │
                       ▼
        ┌──────────────────────────┐
        │   Backend API            │
        │                          │
        │  POST /auth/login        │
        │  POST /auth/register     │
        │  GET /auth/me            │
        │  POST /auth/logout       │
        │                          │
        │ Returns JWT tokens       │
        └──────────────────────────┘
```

---

## 🔄 Authentication Flow Diagram

```
USER FLOW:
┌─────────────────────────────────────────────────────────────────┐
│ 1. App Starts                                                   │
│    └─> Check localStorage for authToken                         │
│        └─> If found: Call getCurrentUser() to validate          │
│            └─> If valid: Set isAuthenticated = true             │
│            └─> If invalid: Clear state, show Login              │
│        └─> If not found: Show Login page                        │
└─────────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│ 2. User Enters Credentials & Clicks "Sign In"                   │
│    └─> Validate form inputs                                     │
│        └─> Call login(email, password)                          │
│            └─> API POST /auth/login                             │
│                └─> Backend validates & returns JWT               │
│                    └─> Redux stores token & user data           │
│                        └─> localStorage.setItem('authToken')    │
│                            └─> Redirect to /dashboard           │
└─────────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│ 3. User Makes API Request                                       │
│    └─> Axios interceptor adds JWT to Authorization header       │
│        └─> GET /api/some-endpoint                               │
│            └─> Header: "Authorization: Bearer <JWT>"            │
│                └─> Backend validates JWT                        │
│                    └─> Return protected data                    │
└─────────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│ 4. User Clicks Logout                                           │
│    └─> Call logout()                                            │
│        └─> API POST /auth/logout                                │
│            └─> Backend clears session                           │
│                └─> Redux clears auth state                      │
│                    └─> localStorage.removeItem('authToken')     │
│                        └─> Redirect to /login                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 Component Relationship Map

```
┌─────────────────────────────────┐
│    useAuth() Hook               │
│  (Consumed by ALL components)   │
└────────--------────────┬────────┘
                         │
         ┌───────────────┼───────────────┐
         │               │               │
         ▼               ▼               ▼
    ┌─────────┐  ┌────────────┐  ┌──────────────┐
    │  Login  │  │  Register  │  │  Dashboard   │
    │  Page   │  │    Page    │  │    Layout    │
    └────┬────┘  └─────┬──────┘  └──────┬───────┘
         │              │                │
         └──────────────┴────────────────┘
                    │
                    ▼
        ┌───────────────────────────┐
        │   Redux authSlice         │
        │                           │
        │ Actions:                  │
        │ - login/pending           │
        │ - login/fulfilled         │
        │ - login/rejected          │
        │ - register/...            │
        │ - logout/...              │
        │ - getCurrentUser/...      │
        └───────────┬───────────────┘
                    │
         ┌──────────┴──────────┐
         │                     │
         ▼                     ▼
   ┌──────────────┐    ┌─────────────────┐
   │ localStorage │    │  API Interceptor│
   │              │    │                 │
   │ authToken    │    │ Add JWT to      │
   │ (persists)   │    │ every request   │
   └──────────────┘    └─────────────────┘
```

---

## 🔐 Security Checklist

- ✅ JWT stored securely (localStorage)
- ✅ Token automatically sent with all API requests
- ✅ Token automatically cleared on logout
- ✅ Invalid tokens detected on app startup
- ✅ 401 responses trigger logout automatically
- ✅ Routes protected from unauthorized access
- ✅ User data fetched and verified on refresh
- ✅ Error messages displayed to user
- ✅ Loading states prevent double submissions
- ✅ Form validation on client side

---

## 📁 File Structure Summary

```
frontend/src/
│
├── main.tsx                          # Redux Provider wrapper
│
├── app/
│   ├── App.tsx                       # Main component + auth check
│   ├── store.ts                      # Redux store configuration ✨ NEW
│   └── routes.tsx                    # Smart routing system ✨ NEW
│
├── features/
│   └── auth/
│       └── authSlice.ts              # Redux auth reducer ✨ NEW
│
├── hooks/
│   └── useAuth.ts                    # Auth hook ✨ NEW & UPDATED
│
├── components/
│   ├── ProtectedRoute/
│   │   ├── ProtectedRoute.tsx        # Route protection ✨ NEW
│   │   └── index.ts                  # Export ✨ NEW
│   └── Loader/
│       ├── Loader.tsx                # Loading spinner ✨ UPDATED
│       └── index.ts                  # Export ✨ UPDATED
│
├── pages/
│   ├── Login.tsx                     # ✨ UPDATED with Redux
│   ├── Register.tsx                  # ✨ UPDATED with Redux
│   ├── ForgotPassword.tsx            # ✨ UPDATED
│   └── NotFound.tsx
│
├── layouts/
│   ├── AuthLayout.tsx                # ✨ UPDATED
│   └── DashboardLayout.tsx           # ✨ UPDATED
│
├── services/
│   ├── api.ts                        # JWT interceptor (was here)
│   └── auth.service.ts               # API calls (was here)
│
├── types/
│   └── auth.ts                       # TypeScript types ✨ NEW
│
└── styles/
    └── ...
```

---

## 🚀 Ready to Use!

Your complete authentication system includes:

1. **Redux Store** - Centralized state management
2. **JWT Handling** - Secure token management
3. **Protected Routes** - Access control
4. **API Integration** - Automatic token injection
5. **User Pages** - Login, Register, Forgot Password
6. **Error Handling** - User-friendly messages
7. **Loading States** - Smooth UX
8. **Type Safety** - Full TypeScript support

---

## 💻 Next Steps

1. **Update backend URLs** in `.env` file
2. **Implement backend endpoints** matching the spec
3. **Customize pages** with your branding
4. **Add more routes** as needed
5. **Test thoroughly** with real backend

---

**All files are created and integrated. You're ready to go! 🎉**
